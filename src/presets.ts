export enum Feedback {
    danger = 'danger',
    warning = 'warning',
    success = 'success',
    info = 'info'
}

export enum Priority {
    critical = 'critical',
    high = 'high',
    medium = 'medium',
    low = 'low',
    minimal = 'minimal'
}

export enum Criticality {
    high = 'high',
    medium = 'medium',
    low = 'low'
}